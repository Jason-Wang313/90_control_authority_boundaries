# Plan

Build paper 90 `control_authority_boundaries` from the shared pool, then perform an expanded-standard v5 submission-readiness audit before publishing:

1. Freeze a CPU-only, RAM-light hostile-review protocol before execution.
2. Implement strong baselines, ablations, stress sweeps, fixed-risk deployment checks, and negative cases.
3. Generate all tables, figures, and a 25+ page ICLR-style manuscript with bright boxed clickable citations.
4. Validate the canonical PDF at `C:/Users/wangz/Downloads/90.pdf` only; do not copy PDFs to the visible Desktop.
5. Update child and root ledgers honestly with the terminal `KILL_ARCHIVE` decision.
